/**
 * Write a description of class Flight here.
 *
 * @Vedha Muvva, Suzy Schwabl, Carolyn Glasener
 * @version (2019-09-17)
 */
public class Flight
{
    public String flightNum;
    public String depCity;
    public String arrCity;
    public String depTime;
    public String arrTime;
    
    /**
     * @param flightNum     This is the flight number
     * @param depCity       This is the departure city
     * @param arrCity       This the arrival city
     * @param depTime       This is the departure time
     * @param arrTime       This is the arrival time
     */
    
    public Flight(String flightNum, String depCity, String arrCity, String depTime, String arrTime)
    {
        this.flightNum = flightNum;
        this.depCity = depCity;
        this.arrCity = arrCity;
        this.depTime = depTime;
        this.arrTime = arrTime;
    }
    
    /**
     * @return  number of flight
     */
    public String getFlightNum()
    {
        return flightNum;
    }
    
    /**
     * @return  departure city
     */
    public String getDepCity()
    {
        return depCity;
    }
    
    /**
     * @return  arrival city
     */
    public String getArrCity()
    {
        return arrCity;
    }
    
    /**
     * @return      the departure time
     */
    public String getDepTime()
    {
        return depTime;
    }
    
    /**
     * @return      the arrival time
     */
    public String getArrTime()
    {
       return arrTime;
    }
    
    /**
     * Phrase the string in a more readable format
     *
     * @return      the departure time
     */
    public String toString()
    {
        return ("flight no: " + flightNum + " from " + depCity + " at " + depTime + "/n" + "arrive in " + arrCity + " at " + arrTime + ".");
    }
    
}
